{{!-- <h2>{{@title}}</h2> --}}

<ul class="list-group">
  {{#each @password as |p|}}  
    {{!-- <li class="list-group-item">{{p.title}}<PasswordsEditButton @password={{p}}/></li> --}}
    <a class="list-group-item list-group-item-action" {{action "edit" p}}>{{p.title}}</a>
  {{/each}}
</ul>
